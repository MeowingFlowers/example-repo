This repository contains a python file.
The file asks the user to enter a sentence, and then stores that sentence as a string.
That string is manipulated to alternate the letters within it between lowercase and uppercase.
The manipulated string is printed twice, once with a lowercase starting letter and once with an uppercase.
